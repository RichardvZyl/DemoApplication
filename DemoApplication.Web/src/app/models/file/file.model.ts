// TODO is this class used to store a file or just define it? looks to just define properties of a file rather than a file itself
export class FileModel {
    constructor(
    public id?: string,
    public name?: string,
    // public contents?: ArrayBuffer | string,
    // public dateTimeLoaded?: Date,
    // public userId?: string
    ) {
    }
}

// part of template does not require any changes at this time.
